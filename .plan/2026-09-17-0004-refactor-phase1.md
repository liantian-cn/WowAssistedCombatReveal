# 重构第一期：从研究脚本到 main.py + app/ 结构（游戏版本 12.1.0.69814）

## Prompt

用户原始请求（第一轮，逐字）：

> 现在帮我重构整个项目
> - 保留MIT协议
> - 对 Shawn McNaughton  https://github.com/shawngmc 致谢
> - 大改
>
> - 根目录保留PROJECT_LOGIC
> - 根目录保留LICENSE
> - 其他根目录的文件都可以删掉了
>
> - 项目改成一个脚本
> - 根目录有main.py，是执行入口
> - app/ 目录存放逻辑代码。
> - csv_table/ 迁移到根目录。我已经操作了。
> - 不再从wowhead读取spellid对应的技能名。csv_table目录下的SpellName文件有映射。（注意该文件很大，不要全部读取）
> - main.py 没有参数，但是包含用于配置的全局变量。目前版本进化到了 12.1.0.69814
> - 输出到output目录。
> - 第一期重构，不该其他逻辑。复用源代码。
> - 新建.venv目录。使用venv ，不再用uv
> - 使用requirements.txt传统包管理。
>
> 原本 tests 目录先清空，用于存放新的tests文件
> 在全部完成后，删掉旧文件和目录
>
> 原本 docs 目录删掉
> 原本 data 目录删掉
> 原本 src 目录删掉

用户补充指令（第二轮，逐字）：

> 客户端是加密的，不要尝试分析exe了。 加载opencode技能，然后配置当前项目的opencode配置
> 添加下面这些references
>
> https://github.com/Gethe/wow-ui-source  ptr分支
> https://github.com/simulationcraft/simc  midnight分支

## Question

### 第 0 轮（事实核对，非提问）
Agent 在仓库中核对到的事实（均来自实际文件，非推测）：

- 仓库状态：git 仓库，HEAD=5662c8b，76 个已跟踪文件（旧结构）；未跟踪：Note.txt、PROJECT_LOGIC.md、根目录 csv_table/（含 11.2.0 与 12.1.0 两套 CSV + 两份 SpellName）、data/csv_table/*.12.1.0*（重复副本）。
- `csv_table/` 根目录副本内容：5 张 12.1.0.69814 表 + 5 张 11.2.0.62493 表 + `SpellName.12.1.0.69814.csv`（11,292,011 字节，414,005 条）+ `SpellName.12.1.0.69814.zhCN.csv`（10,263,200 字节，414,037 条）。
- SpellName 格式：`ID,Name_lang`，值含引号与逗号；enUS 无 BOM；zhCN 为 **GBK 编码**（非 UTF-8）。
- 12.1.0 数据规模：40 个方案 / 693 步 / 3116 条规则 / 60 个专精；新增专精 Demon Hunter / Devourer（specID 1480）；类表新增 Adventurer、Traveler（无方案）。
- 12.1.0 规则实际用到 55 种条件类型；其中 **类型 70 不在 `data/ConditionTypeMap.csv`（覆盖 0–69）内**，共 46 条规则，ConditionValue1/2/3 全为 0。
- 步骤技能 + 规则中"Spell 型参数"去重后共需 552 个技能 ID；其中 **194310 / 389387 / 470058 在 SpellName 中不存在**（分别被 3 条规则引用）。
- 旧输出已存在的缺陷：类型 63/64 的映射 Value1 写作 `Spell Charges`，因以 "spell" 开头被当作技能 ID，旧 `out/*.txt` 出现 `if player has more than 'Spells' charges of spell 'Aimed Shot'` 这类错误行（12 处以上）。
- 本机环境：Python 3.13.15（系统安装）；客户端 `E:\World of Warcraft\_retail_\Wow.exe`，`.build.info` 版本 12.1.0.69814（与数据版本一致）。

### 第 1 轮问答（11 问）

1. **main.py 无参数执行做什么？** → 遍历全部专精（等价旧 run_all.ps1），一次生成全部有辅助方案的专精。
2. **输出文件内容格式？** → 与旧 out/ 完全一致，保留首行的专精名（旧代码 `find_rotation` 里调试 print 的产物）。
3. **Git 入库范围？** → output/ 与 csv_table 全部入库（含两份 SpellName，约 21MB）。
4. **enUS / zhCN 语言？** → 本期只用 enUS；zhCN 文件留在 csv_table 里，后续再接入。
5. **3 个查不到名字的技能 ID 怎么办？** → 输出占位 `Unknown Spell (ID)`，不中断。
6. **12.1.0 出现映射表外的条件类型 70 怎么办？** → 用户反问"我有什么手段知道这个类型70是什么意思？"（见下方调查结果），随后确认：**在映射表补一行说明性文字**（不改渲染逻辑）。
7. **类型 63/64 把充能层数当技能名（旧输出已有 `'Spells' charges of spell` 错误）是否修复？** → 修复（映射表 Value1 改为不以 spell 开头的标签，充能数按数字输出）。
8. **app/ 内部结构？** → 按职责拆多模块；逻辑与旧版等价。
9. **文档与致谢放哪？** → 新建 README.md（安装/运行/数据来源/MIT 与原作者致谢）。
10. **tests/ 放什么？** → 冒烟 + 关键函数单测（约 5~10 个用例）。
11. **清理动作确认（多选）？** → 勾选：删 out/ 旧产物、删 csv_table 里 11.2.0 的 CSV、更新 .gitignore；**未勾选**"删 Note.txt"。

### 第 2 轮问答（2 问）

1. **Note.txt 怎么处理？** → 保留 Note.txt 原样（其内容为 5 张表 + SpellName 的 wago.tools 导出 URL，enUS/zhCN 各一组）。
2. **类型 70 输出形式？** → 映射表补一行说明性文字，渲染逻辑不动。

### 类型 70 调查结论（Agent 调查 + 独立子代理复核，来源：simc midnight 分支本地引用仓库）

- `engine/dbc/data_enums.hh:2053-2126`：`enum assisted_combat_rule_e` 完整列出 0–70，其中 `AC_AUTOMATION_ONLY = 70`；0–69 的成员与仓库映射表 Enum 列逐条一致（仅前缀 `AC_` vs `ASSISTED_COMBAT_RULE_TYPE_` 不同），确认映射表 0–69 编号正确。
- `engine/player/player.cpp`：`AC_AUTOMATION_ONLY` 不产生条件表达式（返回空串），而是在 `parse_assisted_combat_step` 中置 `automation_only = true`；生成的 APL 注释为 "This is for Blizzard automation and is not included in the game's Assisted Combat system."，默认（`use_cds_with_blizzard_action_list=false`）跳过该 step。
- 本地数据复核：46 条类型 70 分布在 46 个不同 step（1 条/step，均为该 step 最后一条条件），覆盖 12 职业 / 27 专精，全部集中在各专精的大爆发技能与少量位移技能；11.2 构建中不存在该类型。
- 结论：类型 70 是"仅属于暴雪自动化（自动施放），不属于游戏 Assisted Combat（高亮/一键循环本体）"的**标记**，不是真正的条件。
- 63/64 交叉验证：simc `AC_SPELL_CHARGES_GREATER(63) → charges >= v1`，v1 为充能层数；12.1 的 10 条 63 规则 v1 全为 2（同一数值而各 step 技能不同，且 SpellName 中无 ID=2 的技能），证实映射表 `Spell Charges` 语义应为层数。

### opencode 配置（用户第二轮指令，已完成）

- 新建 `.opencode/opencode.jsonc`，`references` 增加：
  - `wow-ui-source` → `Gethe/wow-ui-source` @ `ptr`（分支已验证存在）
  - `simc` → `simulationcraft/simc` @ `midnight`（分支已验证存在）
- 两个 reference 的本地 checkout 已就绪（`~/.local/share/opencode/repos/github.com/...`），类型 70 结论即由 simc 引用仓库得出。

## Goal

把 `wow_assist_mapper` 从"可安装 Python 包 + 单文件研究脚本"重构为"脚本型项目"：`main.py`（唯一入口，顶部全局配置）+ `app/`（按职责拆分的逻辑模块）读取 `csv_table/` 下 12.1.0.69814 的 5 张 DB2 导出表与本地 `SpellName` 映射（不再联网抓 wowhead），一次运行为全部 40 个有辅助方案的专精生成 `output/<职业>/<专精>.txt`，并配套 requirements.txt、测试、README、更新后的 PROJECT_LOGIC 与清理后的仓库结构。

## Scope

In：

- 新目录结构：`main.py`、`app/`（4 个模块 + `__init__.py`）、`csv_table/`、`output/`、`tests/`、`README.md`、`requirements.txt`、`ConditionTypeMap.csv`（自 `data/` 迁至根目录）、`.opencode/opencode.jsonc`。
- 技能名来源改为本地 `csv_table/SpellName.12.1.0.69814.csv`：单次流式扫描，只保留本次需要的技能 ID（约 552 个），不整体载入内存。
- 三项已确认的修正：类型 70 映射行、类型 63/64 充能语义修正、技能名缺失占位 `Unknown Spell (ID)`。
- 版本从 11.2.0.62493 切换到 12.1.0.69814（CSV 文件名由 main.py 全局变量拼接）。
- 清理：删除 `docs/`、`data/`、`src/`、`out/`（验证后）、旧根目录模板文件、`csv_table` 中 11.2.0 的 5 个 CSV；清空 `tests/` 旧占位内容。
- 保留：`LICENSE.txt`（MIT 原样）、`PROJECT_LOGIC.md`（改写为新结构）、`Note.txt`（原样）、`.gitignore`（改写为适配新结构）。

Out（本期不做）：

- zhCN 中文输出接入；HTML 可视化；CLI 参数；按 OrderIndex 排序（保持旧行为）；`eval` 模板渲染的替换；EAV/其他表结构扩展；类型 9/13 映射表中 Value2/3 与实际数据不符的修正（记录为遗留问题）。

## Decisions

1. **入口行为**：`main.py` 无参数；执行一次遍历全部职业/专精，为 40 个有 `assist_plan` 的专精各写一个文件到 `output/<职业小写无空格>/<专精小写无空格>.txt`（如 `output/deathknight/blood.txt`）。
2. **输出内容**：与旧 `out/*.txt` 格式完全一致——首行专精显示名（如 `Beast Mastery`），随后 `N: Spell: <技能名>` 与 4 空格缩进的条件行，N 从 0 开始；步骤顺序保持 CSV 行序（不按 OrderIndex 排序）。首行由输出层显式写入，不再依赖旧代码的调试 `print`。
3. **配置全局变量**（main.py 顶部）：`VERSION = "12.1.0.69814"`、`CSV_DIR = "csv_table"`、`OUTPUT_DIR = "output"`、`CONDITION_MAP_FILE = "ConditionTypeMap.csv"`、`LOG_LEVEL = logging.WARNING`；路径相对仓库根目录解析，必须在仓库根目录运行（沿用旧约束）。
4. **技能名索引**：`app/spells.py` 用标准库 `csv` 单次流式扫描 `SpellName.<VERSION>.csv`，只收集"本次需要"的 ID（步骤技能 + 映射表中 ValueN 以 spell 开头的规则的 ConditionValueN，0 除外）；查不到返回 None，渲染为 `'Unknown Spell (<id>)'`。enUS 文件按 UTF-8 读取。
5. **条件映射表**：迁到根目录 `ConditionTypeMap.csv`；新增第 70 行 `70,ASSISTED_COMBAT_RULE_TYPE_AUTOMATION_ONLY,Y,automation only (not part of the game's Assisted Combat rotation),UNUSED,UNUSED,UNUSED`；第 63/64 行 Value1 由 `Spell Charges` 改为 `Charge Count`（输出数字层数）。
6. **未知条件类型**：映射表未覆盖的类型不再让 `rule_props.iloc[0]` 抛 `IndexError`，而是抛出带 ConditionType 与规则 ID 的明确错误（本期实际数据已全部覆盖，此改动只为可诊断性）。
7. **依赖与运行环境**：`.venv`（Python 3.13.15，已创建）+ `requirements.txt`（传统 pip 管理）；运行时依赖 `pandas`（沿用旧版 `>=2.3.1`），测试依赖 `pytest`（在 requirements.txt 中注释标明）。不引入 requests/beautifulsoup4（已无联网抓取）。
8. **模块划分**：`app/db2.py`（读 5 张 CSV 并构建 classes→specs→plan→steps→rules 嵌套结构 + 专精查找）、`app/spells.py`（SpellName 流式索引）、`app/render.py`（条件映射表与文本渲染）、`app/output.py`（命名规则与写文件）、`app/__init__.py`；`main.py` 持有全部配置全局变量并做编排/进度输出。
9. **测试**：`tests/` 用 pytest 写冒烟 + 关键单测（数据规模、SpellName 命中与缺失、充能行渲染、automation-only 行、未知类型报错、整库生成到临时目录）。
10. **文档与归属**：新建 `README.md`（安装/运行/输出说明/数据来源/致谢 Shawn McNaughton https://github.com/shawngmc + MIT）；`PROJECT_LOGIC.md` 按新结构改写，保留"已知问题"章节并补充本期确认的结论与遗留项。数据导出地址等本机备忘不入库（`Note.txt` 由用户确认为敏感信息，`.gitignore` 排除，仅存在于本地工作区，见"Fix round 2"）。
11. **LICENSE**：`LICENSE.txt` 原名原样保留（MIT，作者 Shawn McNaughton）。
12. **清理时机**：先完成实现与新旧输出比对验证，再删除 `out/`、`docs/`、`data/`、`src/` 与旧根目录文件，最后清空并替换 `tests/` 旧占位内容（新测试在实现阶段即写入 `tests/`）。
13. **.gitignore**：保留并改写为适配新结构（忽略 `.venv*/`、`__pycache__/`、`*.py[cod]`、`.pytest_cache/` 等）；不再忽略 `data/*`；**不**忽略 `output/` 与 `csv_table/`（按用户决定入库）；**忽略 `Note.txt`**（本地敏感备忘，见"Fix round 2"）。
14. **opencode 项目配置**：`.opencode/opencode.jsonc` 保存两条 references（已创建，纳入实现提交）。`.opencode/` 目录入库。

## Implementation Steps

1. **环境**：确认 `.venv`（Python 3.13.15）可用；写 `requirements.txt`；在 .venv 中安装 pandas 与 pytest。
2. **映射表**：把 `data/ConditionTypeMap.csv` 移到根目录，修正 63/64 行 Value1，新增 70 行；逐行校验无 `"`/`{`/`}` 等会破坏 `eval('f"…"')` 的字符。
3. **app/ 模块**：按 Decisions 8 实现 `app/db2.py`、`app/spells.py`、`app/render.py`、`app/output.py`、`app/__init__.py`，逻辑自 `src/wow_assist_mapper/skeleton.py` 迁移（保留模板渲染、CSV 读取参数、嵌套结构与命名规则），去掉 wowhead 抓取与 argparse/console-script 相关代码。
4. **main.py**：全局配置变量 + `run()` 编排（加载表 → 构建技能名索引 → 逐专精渲染 → 写文件 → 打印进度与汇总），保证从仓库根目录直接 `python main.py` 可运行。
5. **tests/**：清空旧占位内容，写新测试（见 Decisions 9）并跑通。
6. **文档**：写 `README.md`；改写 `PROJECT_LOGIC.md`（新结构、数据流、模块职责、条件类型 70 与 63/64 结论、遗留问题）。
7. **生成与比对**：运行 `python main.py` 生成 `output/`；对 11.2 与 12.1 共有的专精做新旧输出结构比对（旧 `out/` 与 11.2 数据同源，比对项为格式、编号方式、条件行模板形态，而不仅是文本相等），抽查 warrior/arms、druid/balance、hunter/marksmanship 等。
8. **清理**：`git rm` 旧跟踪文件与目录（docs/、data/、src/、out/、旧根目录模板文件、csv_table 中 11.2.0 CSV），删除未跟踪残留；确认工作区只剩目标结构。
9. **提交**：按工作流两段式提交（计划基线 + 实现）。

## Acceptance Criteria

1. 在仓库根目录执行 `.venv\Scripts\python.exe main.py` 退出码 0，**不产生任何网络访问**，在 `output/` 下生成 **40** 个文件、覆盖 **13** 个职业目录；每个文件名与职业/专精小写无空格命名一致。
2. 每个输出文件：首行为专精显示名；其后为 `N: Spell: <技能名>` 与 4 空格缩进条件行，N 从 0 连续；无空文件、无异常堆栈。
3. 充能类条件（类型 63）输出数字层数，不再出现 `'Spells' charges of spell` 或任何"技能名当层数"的形态。
4. 类型 70 的 46 条规则渲染为映射表新增的说明行 `automation only (not part of the game's Assisted Combat rotation)`（共 46 处）。
5. 3 个缺失技能 ID（194310、389387、470058）渲染为 `'Unknown Spell (<id>)'`，且程序不中断。
6. `.venv\Scripts\python.exe -m pytest tests -q` 全部通过。
7. 技能名索引为单次流式扫描，只保留所需 ID（不把 11MB 文件整体读入 DataFrame/内存缓存）；代码中不存在 requests/bs4/wowhead 相关调用。
8. 仓库最终结构：根目录仅含 `main.py`、`app/`、`csv_table/`、`output/`、`tests/`、`README.md`、`PROJECT_LOGIC.md`、`LICENSE.txt`、`requirements.txt`、`ConditionTypeMap.csv`、`.gitignore`、`.opencode/`、`.venv*/`（外加 `.git/`）；`docs/`、`data/`、`src/`、`out/` 与旧根目录模板文件全部删除；`csv_table/` 只保留 12.1.0.69814 的 5 张表与两份 SpellName。（修订：`Note.txt` 为本地敏感备忘，保留在工作区但被 `.gitignore` 排除，**不入库**，见"Fix round 2"。）
9. 计划与实现提交完成，`git status` 干净；MIT 许可与原作者致谢在 LICENSE.txt 与 README.md 中可见。

## Verification

执行环境：Windows + PowerShell，仓库根目录；`.venv` = Python 3.13.15，pandas 3.0.5、pytest 9.1.1。

### 环境与依赖

- `.venv\Scripts\python.exe -m pip install pandas pytest` → 退出码 0。
- `requirements.txt`：运行依赖 `pandas>=2.3.1`；测试依赖 pytest 以注释标明。
- 未安装除 pandas / pytest 之外的包；运行期无网络访问。

### 验收逐条证据

1. **`main.py` 全量生成（退出码 0 / 无网络 / 40 文件 13 目录）**
   - `.venv\Scripts\python.exe main.py` → 退出码 **0**，用时约 1s；末行汇总 `共生成 40 个文件（技能名索引 548 个，需求 551 个）`。
   - 实测 `output/` = **40** 个 `*.txt`、**13** 个职业目录（含新增 `demonhunter/devourer.txt`）。
   - 确定性：连续两次运行，40 个文件的 SHA256 完全一致（`.script/hashes_before.txt` vs `.script/hashes_after.txt`，Compare-Object 无差异）。
   - 无网络：`main.py` / `app/` / `tests/` 中 grep `requests|bs4|BeautifulSoup|wowhead|urllib|httpx|aiohttp` 无匹配；唯一的 `socket` 出现在 `tests/test_output.py::test_run_is_offline_and_scans_spell_table_once`（把 `socket.socket.connect` / `socket.create_connection` 改为抛错后仍完整生成 40 个文件，属"禁止联网"的护栏测试）。
   - stdout 只有预期告警：2 条 `Field_11_1_7_60520_002 is non-zero`（规则 67752 / 68820，旧版同样告警）+ 1 条 `SpellName 表中缺少 3 个技能 ID`；无异常堆栈。
2. **文件结构**：`tests/test_output.py::test_output_file_structure` 对全部 40 个文件断言首行 = 专精显示名（与文件名对应）、`: Spell: ` 行编号从 0 连续、其余行 4 空格缩进且非空；抽查首行 `Beast Mastery` / `Balance` / `Devourer` / `Assassination` / `Arms`。产物字节形态 = UTF-8（无 BOM）+ CRLF + 末尾换行（`output/warrior/arms.txt` 起始字节 `41 72 6D 73 0D 0A`，94 个换行全为 CRLF），与旧 `out/*.txt` 一致；无空文件、无空行。
3. **充能条件输出数字层数**：全库 `charges of spell` 行 **10** 条，全部形如 `if player has more than 2 charges of spell '<技能名>'`；`'Spells' charges` **0** 处（旧 `out/` 为 10 处）、`'Charge Count'` 当技能名 0 处。单测 `tests/test_render.py::test_charge_rule_renders_number`。
4. **类型 70 说明行**：全库 `automation only (not part of the game's Assisted Combat rotation)` 共 **46** 处（与 46 条规则一致）。单测 `tests/test_render.py::test_automation_only_rule_line`、`tests/test_output.py::test_automation_only_lines`。
5. **缺失技能名占位**：3 条规则分别输出 `if target has buff/debuff 'Unknown Spell (194310)'`（Unholy）、`if player does not have buff/debuff 'Unknown Spell (389387)'`（Mistweaver）、`if player has buff/debuff 'Unknown Spell (470058)'`（Enhancement），程序不中断。
6. **pytest**：`.venv\Scripts\python.exe -m pytest tests -q` → `21 passed in 1.57s`，退出码 **0**。
   （注：清理前旧 `setup.cfg` 的 `addopts = --cov --cov-report` 会因未安装 pytest-cov 直接报错；该文件按计划删除后，验收命令原样通过。）
7. **SpellName 单次流式扫描且只保留所需 ID**：`tests/test_output.py::test_run_is_offline_and_scans_spell_table_once` 统计整库运行只打开 SpellName 表 **1** 次；`tests/test_spells.py::test_index_only_keeps_required_ids` 证明索引只保留 551 个所需 ID 中的 548 个（表中存在的 ID=1 也未被保留）；实现为 `csv.reader` 逐行扫描、命中齐全即停止，不整体载入 DataFrame。
8. **仓库最终结构与清理**：删除一律用 `Remove-Item`（未用 `git rm`）。已删除 `docs/`、`data/`、`src/`、`out/`、`.coveragerc`、`.readthedocs.yml`、`AUTHORS.rst`、`CHANGELOG.rst`、`CONTRIBUTING.rst`、`pyproject.toml`、`README.rst`、`run_all.ps1`、`setup.cfg`、`setup.py`、`tox.ini`、`csv_table/*.11.2.0.62493.csv`（5 个）。保留 `LICENSE.txt`（MIT 原样）、`PROJECT_LOGIC.md`（改写）、`.gitignore`（改写）、`.plan/`（冻结计划）与 `.opencode/`（两条 references）；`Note.txt` 依用户指令按敏感信息处理，仅保留在本地工作区，不入库（见 Fix round 2）。`csv_table/` 现存 12.1.0.69814 的 5 张表 + enUS/zhCN 两份 SpellName（enUS 实测 414,005 数据行 / 11,292,091 字节，与计划调查的 414,005 行一致，字节数较计划记录多 80 字节，属记录笔误量级）。
9. **提交**：按分工未执行 git 写操作（未 add/commit）。`git status --short` 现状 = 73 个已跟踪文件删除 + 3 个修改（`.gitignore`、`tests/conftest.py`、本计划文件的 Verification 段）+ 13 项未跟踪新内容（`app/`、`output/`、`csv_table/`、`main.py`、`README.md`、`PROJECT_LOGIC.md`、`ConditionTypeMap.csv`、`requirements.txt`、`tests/test_*.py`、`Note.txt`）。README.md 含数据来源、MIT 说明与 Shawn McNaughton（https://github.com/shawngmc）致谢。

### 新旧产物比对结论（`.script/compare_outputs.py` → `.script/compare_report.txt`，退出码 0）

- 文件集合：旧 `out/` 39 个 → 新 `output/` 40 个；仅新增 `demonhunter/devourer.txt`，旧文件无缺失。
- 结构：新旧产物**都**通过"首行异常 0 / 编号不连续 0 / 缩进异常 0 / 空文件 0"检查；新产物首行与文件名 100% 对应。
- 步骤与条件行数量逐专精对照（例：`warrior/arms.txt` 21→19 步、87→74 条；`druid/balance.txt` 29→28、144→137；`hunter/marksmanship.txt` 16→17、70→74；`rogue/assassination.txt` 40→35、238→211），差异来自 11.2→12.1 数据本身。
- 条件行"模板形态"：旧 94 种 / 新 96 种，共有 84 种；仅旧数据出现的 10 种（含 `'<spell>' charges of spell` 旧形态、`'s Chill/Mark/Spark/Spear/Fury` 等 11.2 技能名）、仅新数据出现的 12 种（46× automation-only、数字充能层数、能量/月能等资源条件），均为数据差异而非格式差异。
- 旧产物的充能缺陷实例：`if player has more than 'Spells' charges of spell 'Aimed Shot'`、`'Word of Mass Recall (OLD)' charges of spell 'Fire Blast'`、`'Fire Blast' charges of spell 'Phoenix Flames'` 等共 12 处"技能名当层数"；新产物为 10 处数字层数，缺陷清零。

### 已知偏差与说明

- 12.1 与 11.2 数据内容不同（技能增删、步骤/规则数变化、新增 Devourer 与类型 70），文本内容差异属预期；比对口径为格式/编号/模板形态与计数。
- `ChrSpecialization` 的 60 行中只有 54 个专精能挂到职业下（6 个宠物天赋专精 `ClassID=0`），与旧版行为一致；40 个有方案的专精全部在这 54 个之内。
- 计划中"约 552 个所需技能 ID"实测为 **551** 个（去重且去 0 后）。
- 类型 9/13 的参数语义不符问题（类型 13 的 Value3 实为技能 ID、类型 9 有 3+1 条 Value2/3 非 0）按 Scope 记录为遗留问题，写进 PROJECT_LOGIC.md 第 10.4 节，本期未修正。
- 根目录除 Acceptance 8 列出的条目外还有 `.plan/`（冻结计划，基线已提交）与 `.script/`（调查/比对脚本，已写入 `.gitignore` 不入库）；`.pytest_cache/` 由测试生成且已忽略。
- 未执行的检查：未做 11.2 数据的回归运行（旧数据与旧产物已按计划删除）；未接入 zhCN 与类型 9/13 修正（Scope 明确为后续阶段）。

### Fix round 1（复核发现处置）

复核结论：1 Major + 3 Minor + 3 Suggestion。本轮按最小改动处置 R1–R7。

- **R1（Major，已修）`.gitignore` 忽略 `Note.txt`**：删除 `.gitignore` 末尾裸模式 `Note.txt`，
  并把注释改为"output/、csv_table/、Note.txt 等项目文件按约定入库"。
  证据：`git check-ignore -v Note.txt` 退出码 **1**（未被忽略）；`git status --short` 中重新出现
  `?? Note.txt`；`git status --short --ignored` 中已无 `!! Note.txt` 条目。
  **（该项已被用户指令回退：`Note.txt` 为敏感信息，不得入库；`.gitignore` 恢复排除 `Note.txt`，文件从历史中移除，见 Fix round 2。）**
- **R2（Minor，已修）`csv_table/` 实为 6 张 12.1 表 + 2 份 SpellName**：`README.md` 与
  `PROJECT_LOGIC.md`（第 3、4.1 节）补记"另含本期未使用、用户预置的
  `SpellCooldowns.12.1.0.69814.csv`（36,334 数据行）"。
  **已接受偏差**：验收 8 中"5 张表"的口径为**必需的 5 张表均已就位**；目录中另有 1 张用户预置、
  当前无任何代码引用的 `SpellCooldowns` 表（保留，供后续阶段使用）。
- **R3（Minor，已修）README 输出示例不是真实产物**：示例改为原样复制
  `output/hunter/marksmanship.txt`（步骤 0–1 与步骤 12，中间步骤以省略号跳过），
  并在示例前注明来源与"仅中间步骤用省略号跳过"。
  证据：`.script/verify_docs_examples.py` 退出码 0 —— 17/17 行与产物逐行、按序一致。
- **R4（Minor，已修）`find_spec` 丢失 CamelCase 还原**：`app/db2.py` 恢复旧版正则
  `re.sub(r'(?<!^)(?=[A-Z])', ' ', name)` 的还原语义（并合并多余空格），
  显示名与 CamelCase 两种写法都能命中；`tests/test_db2.py` 新增
  `test_find_spec_accepts_camelcase_and_display_names`（`DeathKnight/Blood`、`DemonHunter/Devourer`、
  `Hunter/BeastMastery` 与显示名等价，且仍限定在本职业内）。
- **R5（Suggestion，已修）`eval` 限制表述不准确**：`app/render.py` 与 `PROJECT_LOGIC.md`（7.4、10.3）
  改为"限制只在模板侧（`Description` 不能含 `"`、`\`，`{}` 只能是设计好的占位符）；
  技能名/条件值作为变量值插入，含撇号等字符安全"，并以
  `output/monk/windwalker.txt` 的 `'The Emperor's Capacitor'` 为实证。
- **R6（Suggestion，已修）文档引用不入库的 `.script/`**：`app/render.py` 与 `PROJECT_LOGIC.md`
  不再指引 `.script/inspect_12_1_details.py`，改为把自检步骤与命令直接写进 7.4 节
  （`chr(34)`/`chr(92)` 避免引号嵌套的 pandas 一行式）。该命令已实跑：
  `含危险字符的行: []`，退出码 0；`.script/` 仅在结构清单中标注"已被 .gitignore 排除、不在仓库内"。
- **R7（Suggestion，已修）测试依赖相对路径**：`tests/conftest.py` 用 `ROOT` 拼绝对路径，
  新增 `csv_dir`、`condition_map_file` 夹具，`generated` 与离线测试显式传入；
  `tests/test_db2.py::test_spec_table_scale` 改用 `csv_dir` 夹具。
  证据：在 `C:\Users\liantian\AppData\Local\Temp` 下用绝对路径运行
  `...\.venv\Scripts\python.exe -m pytest E:\...\tests -q` → `22 passed`，退出码 0。

**修复轮回归验证**

- `.venv\Scripts\python.exe main.py` → 退出码 **0**，1.1s，`output/` 仍为 **40** 文件 / **13** 目录；
  40 个文件 SHA256 与修复前（`.script/hashes_after.txt`）**逐字节一致**（`.script/hashes_fix_round1.txt`），
  无渲染/输出回归。stdout 告警仍是 2 条 `Field_11_1_7_60520_002`（4 行）+ 1 条 SpellName 缺失（1 行）。
- `.venv\Scripts\python.exe -m pytest tests -q` → `22 passed`（新增 R4 用例），退出码 0。
- 产物字节数说明：本轮改动只涉及 `.gitignore`、文档、`find_spec`（不被 `run()` 调用）与测试夹具，
  渲染路径未改，因此产物无变化。

### Fix round 2（敏感文件处置，用户指令）

- 触发：用户指出 `Note.txt` 为敏感信息，要求"从提交中删除"，并说明该文件本应由 `.gitignore` 排除。
  Fix round 1 的 R1 处置（把 `Note.txt` 从 `.gitignore` 移除并入库）基于错误前提，予以回退。
- 处置：
  1. `.gitignore` 恢复排除 `Note.txt`（注释标明"本地敏感备忘，仅保留在本地工作区"）。
  2. `README.md` 与 `PROJECT_LOGIC.md` 去掉对该文件的引用；数据来源只描述为 wago.tools 的 DB2 CSV 导出，不再索引任何本地备忘文件。
  3. `git rm --cached Note.txt` 后 `git commit --amend` 改写实现提交：该文件此前**只**出现在被 amend 掉的实现提交中（未 push），改写后 `Note.txt` 不在任何提交里。
  4. 计划文件同步修订：Decisions 10/13、Acceptance 8、Verification 8、Review Notes 与 Completion。
- 证据（执行后核对）：`git ls-files Note.txt` 为空；`git log --all --oneline -- Note.txt` 为空；
  `git log --all -S` 与 `git grep HEAD` 检索"导出路径片段 / 查询参数"均无匹配——字面地址未进入任何提交
  （检索串不在本文件中复述，以免自命中）；`git check-ignore -v Note.txt` 命中 `.gitignore` 规则；
  `git status --short` 干净；`Note.txt` 仍存在于本地工作区（内容未改动）；
  改写产生的悬空对象已用 `git reflog expire --expire=now --all` + `git gc --prune=now` 清除
  （原地验证：被 amend 掉的实现提交对象已不可读取）。
- 影响面：仅仓库跟踪状态与文档表述；`main.py`、`app/`、`output/` 与测试均未改动，无需重跑验证。

## Review Notes

### Round 1（全量复核）

- 复核基线：`git diff HEAD`（当时 HEAD 为用户自行提交的 `61bcbee`）+ 未跟踪新增文件；对照旧实现 `git show 5662c8b:src/wow_assist_mapper/skeleton.py`。
- 结论：无 Blocker；**1 Major（R1）+ 3 Minor（R2–R4）+ 3 Suggestion（R5–R7）**。
- 独立确认（Reviewer 自查，非实施者自述）：三处已批准修正（类型 70 映射行、63/64 充能语义、缺失技能名占位）实现正确且无副作用；`app/` 对旧逻辑的迁移在 CSV 读取参数、嵌套结构与键、步骤遍历顺序（CSV 行序）、模板渲染语义、职业/专精匹配、命名规则上忠实等价；`ConditionTypeMap.csv` 除 63/64/70 三处外逐行未变且无破坏 `eval('f"…"')` 的字符；CSV 侧统计（693 步 / 3116 规则 / 46 条类型 70 / 10 条类型 63 / 3 个缺失 ID）与产物逐条吻合。
- R1（Major）：`.gitignore` 新增了裸模式 `Note.txt`，会让交付仓库缺少该文件 → 阻塞交付，进入修复轮。
  **[前提有误：`Note.txt` 是用户的敏感文件，本就不应入库；该发现与处置已按用户指令回退，见 Fix round 2。]**
- 限制：Reviewer 会话的 shell 除只读 git 子命令外被权限拒绝，无法实跑 `pytest`/`main.py`；改用代码审查 + `git grep --no-index` 静态核验（CRLF、行数、逐文件统计、逐行比对）。

### Round 2（修复复核：仅 R1–R7 与其直接回归）

- R1–R7 **全部判定"已解决"**；未发现新的 Blocker / Major / Minor。
- R1 证据：`git status --short` 重新出现 `?? Note.txt`，`git status --short --ignored` 中不再有 `!! Note.txt`，且 `.gitignore` 无任何变相匹配 `Note.txt` 的模式；`output/`、`csv_table/` 仍未被忽略。 **[该项处置已按用户指令回退：`Note.txt` 为敏感信息，恢复忽略并从提交中移除，见 Fix round 2。]**
- R3 证据：README 示例 17 行经 Reviewer 逐行比对，在 `output/hunter/marksmanship.txt` 中按序存在，未拼接不存在的行。
- R4 证据：`find_spec` 归一化后显示名与 CamelCase 两种写法均命中，仍限定本职业（`Hunter/Frost → None`），职业间与职业内无同义歧义；`find_spec` 无生产调用点，渲染路径不受影响。
- 回归面：`main.py`、`app/spells.py`、`app/output.py`、`app/__init__.py`、渲染逻辑逐行未变；`pytest` 用例数 21→22（新增 R4 用例）；产物逐文件行数与关键行分布（`charges of spell` 10 / `automation only` 46 / `Unknown Spell` 3）与复核前一致。
- 新增 2 条 Suggestion（按规则不阻塞交付，未触发返工，记录为残留项）：
  - **N1**：`README.md` 示例中的 `    ...` 行带 4 空格缩进、非产物原文，与"原样复制"的表述略有张力（示例前已注明"中间步骤用省略号跳过"）。
  - **N2**：`tests/conftest.py` 的 `repo_root` 夹具当前无任何用例引用。
- **已接受偏差（PuttyCoder 确认）**：验收 8 中"5 张表"的口径为"必需 5 张表均已就位"；`csv_table/` 额外保留用户预置、当前无代码引用的 `SpellCooldowns.12.1.0.69814.csv`（971 KB，用户 2026-09-17 新增，后续阶段使用）。
- 审查流程说明：两轮复核均未修改任何文件、未执行 git 写操作；Reviewer 未代为接受风险决策，偏差口径由 PuttyCoder 明确。

## Completion

- **交付内容**：`main.py`（唯一入口 + 顶部全局配置）、`app/`（`db2.py` / `spells.py` / `render.py` / `output.py` / `__init__.py`）、`ConditionTypeMap.csv`（根目录，含 70 行新增与 63/64 修正）、`requirements.txt`、`tests/`（22 用例）、`README.md`、改写后的 `PROJECT_LOGIC.md` 与 `.gitignore`、`output/`（40 文件 / 13 职业目录）；旧结构 `docs/`、`data/`、`src/`、`out/` 与 11 个根目录模板文件、`csv_table/` 中 11.2.0 的 5 个 CSV 已删除；`LICENSE.txt`（MIT 原样）保留；`Note.txt` 按用户指令作为本地敏感备忘、不入库（`.gitignore` 排除，见 Fix round 2）。
- **提交**：`32f5932` 计划基线；`61bcbee` 用户自行提交的 `.opencode/opencode.jsonc`；实现提交（含 Fix round 2 的敏感文件处置，经 `--amend` 改写，最终哈希见交付说明）。
- **验收状态**：计划 Acceptance 1–9 全部满足，证据见 `## Verification` 与 `## Review Notes`。其中验收 1（退出码/无网络/40 文件）与验收 6（pytest 22 passed）的可执行证据由 Developer 提供并两次复现；Reviewer 因会话权限限制未能实跑，改用代码审查 + 产物静态核验交叉确认，未发现矛盾。
- **残留风险与未做项**：zhCN 未接入（文件保留）；类型 9/13 参数语义未修正（见 `PROJECT_LOGIC.md` 10.4）；输出顺序仍为 CSV 行序；`eval` 模板渲染保留；未做 11.2 数据回归（旧数据与旧产物已删除）；N1/N2 两条 Suggestion 未处理；`.script/`（调查/比对脚本与产物）保留在本地并被 `.gitignore` 排除，不入库。
