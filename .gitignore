# 临时与二进制文件
*~
*.py[cod]
*.so
*.orig
*.log
*.pot
__pycache__/
*.egg-info
.DS_Store

# 虚拟环境（.venv 由项目自行创建，不入库）
.venv*/
.conda*/
.python-version

# 测试与覆盖率
.pytest_cache/
htmlcov/
.coverage
.coverage.*
.tox
coverage.xml

# 编辑器/IDE
.idea/
.vscode/
*.swp
tags

# 调查与比对脚本的暂存目录（内容为一次性分析脚本与输出，不入库）
.script/

# 本地敏感备忘（数据导出地址），仅保留在本地工作区，不入库
Note.txt

# 注意：output/ 与 csv_table/ 按项目约定入库，不要在这里忽略
